# OpenGL 1.1 (GLFW) Triangle sample for Linux (Rust)

## Description

OpenGL 1.1 triangle using vertex arrays with GLFW window management.

## Requirements

- Rust (rustc, cargo)
- libglfw3-dev

## Build

```sh
cargo build --release
```

## Run

```sh
./target/release/hello
```