# Hello, OpenGL 1.0 World! (D + GLUT)

## Build

```sh
chmod +x build.sh
./build.sh
```

## Run

```sh
./hello
```

## Requirements

- GCC (`gcc`)
- GDC (`gdc`)
- OpenGL (`libGL`)
- FreeGLUT (`libglut`, `libGL-dev`, `freeglut3-dev`)
