compile:
```
C:\> php hello.php
```
Result:
```
+-------------------------------+
|   0123456789ABCDEF          X |
|                               |
| Hello, WinRT (PHP) World!     |
|                               |
+-------------------------------+
```

Note:
- This sample uses PHP FFI. Enable it in `php.ini` (e.g. `ffi.enable=1`).
